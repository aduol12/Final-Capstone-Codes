specVersion: "v1"
serviceId: "web2"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "web2-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
